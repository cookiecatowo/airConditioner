# 空調安裝維修報價系統 - 開發手冊 (GEMINI.md)

## 專案概述
本專案為一套專為空調工程設計的報價與管理系統，採用 **Laravel 12**, **Vue 3 (Inertia.js)**, 與 **Tailwind CSS** 構建。

## 技術架構
- **後端**: Laravel 12 / PHP 8.3+
- **前端**: Vue 3 (Composition API) + Inertia.js
- **資料庫**: SQLite (預設)
- **報表**: 使用 `phpoffice/phpword` 產出 Word 格式報價單。

## 資料庫核心邏輯
1. **Snapshots (快照機制)**: 
    - `orders` 表記錄施工當下的地址、統編。
    - `order_equipment` 與 `order_material` 記錄成交當下的價格與規格。
2. **手動調整項 (Adjustments)**: 
    - 允許在報價單加入非資料庫現有項目（如：折扣、特殊工資）。
    - **技術實作**: `equipment_id` 與 `material_id` 設為 `nullable`。
    - **儲存/讀取**: 避開 Eloquent 關聯，使用 `DB::table` 直接操作以支援 `NULL` ID。
3. **兩階層設備管理**: 品牌 -> 設備系列 -> 具體規格。

## UI 與 報表規範 (核心)
### 1. 報價單檢視 (Show.vue)
- **雙重佈局**: 上方為網頁版資訊卡（快速檢視），下方為模擬 A4 紙張預覽（高度還原 PDF）。
- **紙張模擬**: 
    - A4 比例 (`210mm x 297mm`)，具備紙張陰影。
    - 抬頭資訊使用「下底線 (Underline)」公文樣式。
    - 字體優先選用「新細明體」。
- **表格結構 (7 欄位)**:
    - 欄位：項目 | 品名 | 規格 | 數量(含單位) | 單價 | 金額 | 備註。
    - **跨行合併**: 「項目」欄位在表身中為垂直合併的空白儲存格。
    - **文字對齊**: 表格內所有文字一律靠中對齊。
    - **總計/小計**: 小計與總計列將「金額」與「備註」欄位合併 (`colspan="2"`)。

### 2. 連動搜尋與輸入
- **顧客連動**: 輸入姓名自動帶入電話、統編與地址。
- **設備連動**: 選取品牌後，型號自動帶入品牌前綴。
- **動態行**: 材料輸入完最後一行會自動新增下一行。

## 待辦事項 (TODO)
- [x] **Word 報價單導出**: 整合 `phpoffice/phpword`，初步完成。
- [x] **調整項邏輯修復**: 支援 `NULL` ID 儲存與編輯/檢視頁面正確載入。
- [x] **報價單詳情頁 (Show)**: 實作高度還原 PDF 的檢視頁面。
- [ ] **報價單搜尋優化**: 實作列表頁按顧客姓名、地址、電話的進階過濾。
- [ ] **Word 導出樣式精進**: 確保 Word 檔案與 `Show.vue` 的預覽效果一致。

## 開發指令參考
- 啟動後端: `php artisan serve`
- 啟動前端: `npm run dev`
- 資料庫遷移: `php artisan migrate`
- 建立新 Model: `php artisan make:model Name -m`
